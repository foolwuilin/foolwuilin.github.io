---
import '../styles/global.css';
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
const { title, description, image, type, jsonld, lang = 'en', alternates = [] } = Astro.props;
---
<!doctype html>
<html lang={lang}>
  <head>
    <BaseHead title={title} description={description} image={image} type={type} jsonld={jsonld} lang={lang} alternates={alternates} />
  </head>
  <body>
    <Header />
    <slot />
    <Footer />
  </body>
</html>
